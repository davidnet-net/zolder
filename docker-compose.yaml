services:
    sveltekit:
        build: .
        container_name: zolder
        ports:
            - "5137:3000"
        restart: unless-stopped
        dns: 192.168.1.141
        volumes:
            - data:/app/data

volumes:
    data:
